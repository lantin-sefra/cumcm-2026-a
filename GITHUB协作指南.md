# 三人 GitHub 协作指南（零基础）

这份说明给国赛三人队用。你用 Cursor，另两人用 Codex。按钮不一样，规则一样。

网上仓库地址（私有，只有被邀请的人能打开）：

https://github.com/lantin-sefra/cumcm-2026-a

第一次复制到自己电脑时用的地址：

`https://github.com/lantin-sefra/cumcm-2026-a.git`

---

## 先认识会反复出现的词

- **GitHub**：一个把项目放在网上的网站，用来备份、同步、查看历史版本。
- **Git**：装在电脑上的版本记录工具，负责记下「谁在哪次改了什么」；GitHub 是它常用的网上存放处。
- **仓库（repository，简称 repo）**：项目在 GitHub 上的那个文件夹，三人共用这一份。
- **私有（private）**：只有被邀请的人能看见和下载，网上路人打不开。
- **克隆（clone）**：把网上的仓库完整复制到自己电脑，通常每人只做一次。
- **拉取（pull）**：开工前把网上已有的最新改动下载到自己电脑。
- **提交（commit）**：把这一小段修改在本地记下，并写一句「改了什么」。
- **推送（push）**：把已经提交的记录上传到 GitHub，队友才能看见。
- **分支（branch）**：同一项目里互不干扰的一条修改线；你们平时只用主线 `main`。
- **冲突（conflict）**：两个人改了同一文件的同一处，Git 无法自动合并，需要人来选留哪一版。
- **标签（tag）**：给某一次提交起一个好记的名字，例如「交稿前终版」，方便回头找。

---

## 三个人只记三条

1. **动手前先拉取（pull）**：把网上最新版下载到自己电脑，避免在过期副本上改。
2. **改完一小块就提交（commit）并推送（push）**：不要攒到晚上一次性传。
3. **尽量不要同时改同一个文件**：用下面的目录分工；必须同时改时，开个人分支。

建议目录分工（可按实际角色微调）：

| 谁 | 软件 | 主要改哪里 |
|---|---|---|
| 队员 A | Cursor | `workspace/paper/` 论文 |
| 队员 B | Codex | `workspace/code/` 求解代码 |
| 队员 C | Codex | `workspace/figures/` 作图与审校 |

**主分支**只用 `main`。主分支是大家默认看到、默认上传的那一条线。平时都往 `main` 推送。

---

## 0. 每人先准备

- 注册 GitHub：https://github.com/signup
- 安装 Git（Windows 可装 Git for Windows；Cursor / Codex 往往已自带）
- 队员 B、C 把 **GitHub 用户名**发给仓库主人。用户名是注册时公开的那个英文 ID，例如 `zhangsan`，不是登录邮箱。

---

## 1. 仓库主人：邀请另两人

仓库主人就是创建这个仓库、并有权邀请别人的那个人。

1. 打开 https://github.com/lantin-sefra/cumcm-2026-a
2. 点 **Settings**（这个仓库的设置页）
3. 左侧点 **Collaborators**（协作者名单：被请来一起改这个仓库的人）
4. 点 **Add people**，输入队友的用户名
5. 权限选 **Write**。Write 的意思是：可以下载、修改并上传，但不能删除整个仓库或改最高权限。
6. 对方邮箱会收到邀请，必须点 **Accept**（接受邀请）。不点的话，对方上传会被拒绝。

---

## 2. 队员 B / C：第一次拿到项目（Codex）

**克隆（clone）** 的意思是：把网上的仓库完整复制到自己电脑，通常每人只做一次。

把下面整段发给 Codex：

> 用 git clone 把这个私有仓库复制到本地：`https://github.com/lantin-sefra/cumcm-2026-a.git`，然后打开项目。之后每次动手前先 git pull（下载最新版）；改完后 commit（记下修改）并 git push（上传）。不要 force push（强行覆盖网上的版本），不要把 .gitignore 里忽略的 `_tmp` 加进去。

第一次推送或拉取会弹出 GitHub 登录，用浏览器授权即可。授权的意思是：允许这台电脑以你的身份访问这个私有仓库。

如果网页克隆时提示没有权限，先确认已经点过邀请邮件里的 Accept。

---

## 3. 你每天在 Cursor 里怎么同步

- 左侧 **Source Control**（源代码管理面板，图标是两个分支分叉；用来看改了哪些文件、提交和上传）
- 先点 **⋯ → Pull**（拉取：下载网上最新版）
- 改文件
- 在消息框写一句人话（如「修正问题3烘干时长表述」）→ **Commit**（提交：记下这一小段修改）→ **Sync / Push**（同步/推送：把记下的内容上传到 GitHub）

也可以直接对 Cursor 里的 AI 说：「先拉取，提交当前修改并推送」。

---

## 4. 队员 B / C 每天对 Codex 说的话（复制即用）

开始干活前：

> 先 `git pull`（把网上最新版下载下来），不要改别人正在写的文件。我今天只改 `workspace/code/`。（把路径换成你今天负责的目录）

干完一块：

> 把我改过的文件提交（commit：记下改了什么），写清楚改了什么，然后 `git push`（上传到 GitHub）。不要 `--force`（不要强行覆盖别人刚上传的内容），不要把 `workspace/_tmp` 加进去。

---

## 5. 保存「重要版本」（比赛节点）

在关键节点打 **标签（tag）**。标签是给某一次提交起的好记名字，例如：`v-model-ok`、`v-paper-draft`、`v-submit`。

可以让 Cursor 或 Codex 说：

> 给当前版本打标签 `v-paper-draft` 并推送到 GitHub。不要 force。

GitHub 网页 → **Commits**（提交历史：每一次保存记录的列表）/ **Tags**（标签列表）可以看到每一版；点进某次提交可以下载当时的全部文件。

---

## 注意事项（务必让三个人都看到）

- **必须私有**：私有表示只有被邀请的人能看见。比赛期间不要点 **Make public**（把仓库变成任何人都能打开），不要把仓库加到公开组织。
- **只邀请本队三人**：Collaborators（协作者名单）不要加朋友「帮忙看一眼」。
- **禁止对 `main` 做 force push**。force push（`git push --force` 或 `--force-with-lease`）的意思是：强行用你电脑上的版本覆盖网上的版本，别人刚上传的内容会被抹掉。若 Codex/Cursor 自己提议 force，一律拒绝。
- **先拉取再改**：忘了拉取就改，最容易出现 **冲突**。冲突（conflict）是两个人改了同一文件的同一处，Git 无法自动决定留哪一版。
- **小步提交**：一次提交只做一类事（改公式 / 改代码 / 改图），以后要 **回退**（回到某个旧版本）才找得到。
- **不要同时让两个 AI 重写同一文件**：Cursor 和 Codex 都爱整文件覆盖，这是三人协作最大风险。`main.tex`、`params.py` 尤其危险。
- **冲突了怎么办**：停下来，先 `git pull`；若提示 conflict，把冲突文件发给队友或让 AI「只解决冲突、不要重写整篇论文」。不要选「用我的版本全部覆盖」。
- **单文件不要超过 50MB**：GitHub 对过大文件会警告（50MB）或直接拒绝（100MB）。本仓库最大隐患是 `workspace/_tmp/mik/`（那是一套 TeX 排版软件的安装文件），已经写进 `.gitignore`，不要手动加进去。
- **中文路径**：本机文件夹名带「A题」一般没问题；仓库名用英文 `cumcm-2026-a`。
- **国内网络**：clone（第一次复制）或 push（上传）失败时换网络或稍后重试，不要把来路不明的「加速器脚本」塞进仓库。
- **回退**：想回到昨天晚上的版本，用 GitHub 网页打开那次提交，或让 AI「恢复到某某提交，但先不要 push」，确认没问题再推送。

---

## 仓库里故意不上传的内容

`.gitignore` 是一份清单，告诉 Git 哪些文件不要上传。当前忽略了：

- `workspace/_tmp/`：编译/审校中间文件，以及本机 TeX 安装
- Python 缓存、虚拟环境
- 系统垃圾文件（`Thumbs.db` 等）
- Cursor 本机状态（`.cursor/`）

代码、论文 `.tex`、图表脚本、`output/` 结果、赛题附件会进仓库，三人都能拿到。
